#include "simple-renderer/buffer_ref.hpp"

#include <stdexcept>

namespace simple {

void WriteToBuffer::operator()(const RBufferRef &readable_buffer)
{
    if (readable_buffer.getSize() < )
}


} // simple